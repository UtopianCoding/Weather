<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atigu.weather.mapper.AirQualityPollutantMapper">
    <sql id="Base_Column_List">
        id,
        day_id,
        code,
        `name`,
        full_name,
        concentration_value,
        concentration_unit
    </sql>
    <resultMap id="BaseResultMap" type="com.atigu.weather.pojo.AirQualityPollutant">
        <result column="id" property="id"/>
        <result column="day_id" property="dayId"/>
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="full_name" property="fullName"/>
        <result column="concentration_value" property="concentrationValue"/>
        <result column="concentration_unit" property="concentrationUnit"/>
    </resultMap>

<!--auto generated by MybatisCodeHelper on 2025-04-15-->
    <select id="findAll" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from air_quality_pollutants
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
            insert into  air_quality_pollutants (day_id, code, `name`, full_name, concentration_value, concentration_unit)
            values (#{dayId}, #{code}, #{name}, #{fullName}, #{concentrationValue}, #{concentrationUnit})
    </insert>
</mapper>
