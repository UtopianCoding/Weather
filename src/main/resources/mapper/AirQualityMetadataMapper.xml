<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atigu.weather.mapper.AirQualityMetadataMapper">
    <sql id="Base_Column_List">
        id,
        tag,
        created_at
    </sql>
    <resultMap id="BaseResultMap" type="com.atigu.weather.pojo.AirQualityMetadata">
        <result column="id" property="id"/>
        <result column="tag" property="tag"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>


<!--auto generated by MybatisCodeHelper on 2025-04-15-->
    <select id="findAllById" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from air_quality_metadata
        where id=#{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into air_quality_metadata (tag, created_at)
        values (#{tag}, #{createdAt})
    </insert>

</mapper>
